#include <stdio.h>
/**
 * main - entry point
 * progiming in c
 * testing betty
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
